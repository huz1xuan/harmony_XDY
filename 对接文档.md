# 学点云课堂 HarmonyOS SDK 集成文档
（该文档由AI生成经过人工审核）
## 一、概述

学点云课堂 SDK（`@xdy/classroom`）是一个 HarmonyOS HAR 库，集成了实时音视频通讯、WebView 课堂展示和 Native RTC 引擎控制能力。客户只需在自己的 App 中引入组件即可启动课堂。

**核心特性：**
- 一行代码启动课堂，免登录进入
- 独立 UIAbility 运行，横屏全屏，不影响客户 App 主流程
- 集成实时音视频（基于声网 Agora RTC）
- 支持屏幕共享、多人互动等教学场景

**两种集成模式：**

| 模式 | 集成成本 | 窗口 | 适用场景 |
|------|---------|------|---------|
| Ability 模式 | 1 文件 + 1 JSON | 独立横屏窗口 | 需要独立课堂窗口的客户 |
| 嵌入式模式 | **零文件零配置** | 嵌入当前页面 | 无需独立窗口，最小集成 |

> **集成说明：** 从 API 14 开始，HAR 支持在配置文件中声明 `UIAbility`（实测 DevEco Studio 6.0.1 + Hvigor 可用），Ability 自动合并到宿主 App，客户不再需要手动注册。但 `pages` 页面因相对路径冲突（架构限制）仍需在 entry 的 `main_pages.json` 中手动注册。嵌入式模式直接使用 `ClassroomView` 组件，无需任何页面注册或文件创建，做到真正零配置。

**技术要求：**
| 项目 | 要求 |
|------|------|
| 操作系统 | HarmonyOS 6.0.1+ |
| API Level | 21+ |
| 开发工具 | DevEco Studio 5.0+ |
| 设备类型 | 手机、平板 |

---

## 二、SDK 包内容

SDK 为自包含 HAR 文件，内嵌所有依赖：

| 文件 | 大小 | 说明 |
|------|------|------|
| `classroom.har` | ~22MB | 课堂核心模块（ClassroomView 组件、XDYClassroom 启动类、JS Bridge、视频渲染等），内嵌 xrtc 音视频引擎及 Agora RTC SDK |

只需将 `classroom.har` 放入客户项目的 `libs/` 目录。

---

## 三、集成方式

### 方式一：Ability 模式（独立窗口 · 1 文件 + 1 JSON）

课堂以独立 UIAbility 窗口启动，横屏全屏，不影响客户页面流。

#### 步骤 1：添加依赖

将 `classroom.har` 放入 `libs/`，在 entry 的 `oh-package.json5` 中添加：

```json5
{
  "dependencies": {
    "@xdy/classroom": "file:./libs/classroom.har"
  }
}
```

> classroom.har 已自包含音视频引擎等所有依赖，无需额外引入。

执行 `ohpm install`。

#### 步骤 2：创建课堂页面

创建 `entry/src/main/ets/pages/ClassroomPage.ets`：

```typescript
import { ClassroomView } from '@xdy/classroom';

const localStorage: LocalStorage = new LocalStorage();

@Entry(localStorage)
@Component
struct ClassroomPage {
  aboutToAppear(): void {
    const url = AppStorage.get<string>('xdy_classroom_url');
    if (url && url.length > 0) {
      localStorage.setOrCreate('classroomUrl', url);
    }
  }

  build() {
    Stack() {
      ClassroomView()
    }
    .width('100%')
    .height('100%')
  }
}
```

在 `entry/src/main/resources/base/profile/main_pages.json` 中注册：

```json
{
  "src": [
    "pages/Index",
    "pages/ClassroomPage"
  ]
}
```

> **注意：** 无需在 `module.json5` 中注册 XDYClassroomAbility——API 14 起 HAR 的 Ability 自动合并。

#### 步骤 3：声明权限（可选）

在 entry 的 `module.json5` 中添加以下权限（XDYClassroomAbility 由 HAR 自动合并，需在 `usedScene` 中显式声明）：

```json5
"requestPermissions": [
  { "name": "ohos.permission.INTERNET" },
  { "name": "ohos.permission.CAMERA", "reason": "$string:camera_reason",
    "usedScene": { "abilities": ["EntryAbility", "XDYClassroomAbility"], "when": "inuse" } },
  { "name": "ohos.permission.MICROPHONE", "reason": "$string:microphone_reason",
    "usedScene": { "abilities": ["EntryAbility", "XDYClassroomAbility"], "when": "inuse" } }
]
```

#### 步骤 4：启动课堂

```typescript
import { common } from '@kit.AbilityKit';
import { XDYClassroom } from '@xdy/classroom';

const ctx = getContext(this) as common.UIAbilityContext;
XDYClassroom.launch(ctx, 'https://xxx?mobileWeb=true&...');
```

---

### 方式二：嵌入式模式（零文件零配置）

> **前提：** 与方式一相同，需完成步骤 1（添加依赖）和步骤 3（权限声明）。步骤 2（创建页面 + 注册 main_pages.json）**无需执行**。

通过 `ClassroomView` 组件直接嵌入客户页面，无需独立窗口，无需任何文件复制或 JSON 修改。

```typescript
import { ClassroomView } from '@xdy/classroom';

const localStorage: LocalStorage = new LocalStorage();

@Entry(localStorage)
@Component
struct MyPage {
  @State showClassroom: boolean = false;

  build() {
    if (this.showClassroom) {
      Stack() {
        ClassroomView()
        Button('← 返回').onClick(() => this.showClassroom = false)
      }
      .width('100%')
      .height('100%')
    } else {
      Button('进入课堂').onClick(() => {
        localStorage.setOrCreate('classroomUrl', 'https://xxx?mobileWeb=true&...');
        this.showClassroom = true;
      })
    }
  }
}
```

> **原理：** `ClassroomView` 通过 `@LocalStorageProp('classroomUrl')` 读取 URL。只要父级 `@Entry(localStorage)` 提供了 LocalStorage 并写入 `classroomUrl`，即可正常启动课堂。

两种模式可并存，客户根据场景选用。

---

## 四、API 参考

### XDYClassroom

| 方法 | 说明 |
|------|------|
| `static launch(context, url, abilityName?)` | 启动课堂（Ability 模式，独立窗口） |
| `static pushTo(navPathStack, url)` | 启动课堂（Navigation 嵌入模式） |

**launch 参数说明：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| context | UIAbilityContext | 是 | 调用方的 UIAbilityContext |
| url | string | 是 | 完整的课堂 H5 URL（含鉴权 token） |
| abilityName | string | 否 | 课堂 Ability 名称，默认 `'XDYClassroomAbility'` |

**pushTo 参数说明：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| navPathStack | NavPathStack | 是 | 客户 Navigation 的 NavPathStack |
| url | string | 是 | 完整的课堂 H5 URL（含鉴权 token） |

### Navigation 集成

| 导出 | 类型 | 说明 |
|------|------|------|
| `classroomNavBuilder` | @Builder | Navigation 路由构建器，通过 `.navDestination('xdy_classroom', wrapBuilder(classroomNavBuilder))` 注册 |
| `ClassroomNavContent` | @Component | NavDestination 内容组件，从路由参数 `classroomUrl` 读取 URL（也可直接使用） |

### ClassroomView

| 组件 | 说明 |
|------|------|
| `ClassroomView` | 课堂核心 UI 组件，包含 WebView、音视频渲染、JS Bridge 等全部功能 |

**使用方式：**
```typescript
import { ClassroomView } from '@xdy/classroom';

// 在 @Entry 页面中使用（需包裹在容器组件中，并提供 LocalStorage）
@Entry(localStorage)
@Component
struct MyPage {
  build() {
    Stack() {
      ClassroomView()
    }
    .width('100%')
    .height('100%')
  }
}
```

**LocalStorage 参数：**

| Key | 类型 | 说明 |
|-----|------|------|
| classroomUrl | string | 课堂URL（可选，为空时使用默认登录页） |

---

## 五、课堂 URL 说明

课堂 URL 由您的后端服务生成，包含用户鉴权信息，用户无需在课堂内再次登录。

> **注意：** 如果 `ClassroomPage` 的 LocalStorage 中未设置 `classroomUrl`（即不传入课堂 URL），课堂将进入学点云登录页面，用户需手动登录。**务必在 URL 中拼接 `mobileWeb=true` 参数**，否则课堂将以H5模式加载，导致布局异常。

**常见参数：**

| 参数 | 说明 |
|------|------|
| mobileWeb | 固定为 `true`（**必填**，移动端标识，缺失将导致H5特殊布局） |
| courseId | 课程/课堂 ID |
| appVersion | 应用版本号（可选） |

> 具体 URL 参数请联系学点云技术支持获取对接文档。

---

## 六、架构说明

```
┌──────────────────────────────────────────────┐
│  客户 App                                     │
│  - 业务页面                                   │
│  ┌──────────────────────────────────────────┐│
│  │ 方式一：XDYClassroom.launch(url)          ││
│  │         → 独立 Ability 窗口               ││
│  │ 方式二：XDYClassroom.pushTo(stack, url)   ││
│  │         → Navigation 嵌入                ││
│  └──────────────────────────────────────────┘│
└─────────────────┬────────────────────────────┘
                  │
                  ▼
┌──────────────────────────────────────────────┐
│  XDYClassroomAbility（方式一：独立窗口横屏）   │
│  ┌────────────────────────────────────────┐  │
│  │  ClassroomPage（薄包装 @Entry）         │  │
│  │  └─ ClassroomView (from HAR)           │  │
│  │     ┌────────────────────────────┐     │  │
│  │     │ WebView (课堂 H5 页面)      │     │  │
│  │     └──────────────┬─────────────┘     │  │
│  │                    │ JS Bridge          │  │
│  │     ┌──────────────▼─────────────┐     │  │
│  │     │ Native 控制层               │     │  │
│  │     │ - 音视频流管理              │     │  │
│  │     │ - 视图渲染 (XComponent)    │     │  │
│  │     │ - 屏幕共享                  │     │  │
│  │     └──────────────┬─────────────┘     │  │
│  │                    │                    │  │
│  │     ┌──────────────▼─────────────┐     │  │
│  │     │ xrtc SDK (内嵌于 HAR)       │     │  │
│  │     │ - RTC 引擎 (声网 Agora)    │     │  │
│  │     └────────────────────────────┘     │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

**关键特点：**
- 课堂运行在独立的 UIAbility 中（方式一）或客户 Navigation 页面流中（方式二）
- ClassroomView 是 HAR 导出的 @Component，所有逻辑自包含
- 方式一：客户只需创建薄包装页面（Ability 由 HAR 自动提供）
- 方式二：客户只需注册 `classroomNavBuilder` 到 Navigation（零文件零配置）
- 方式一退出课堂自动关闭 Ability，方式二通过 Navigation 返回

---

## 七、退出课堂

课堂退出由 H5 页面内部触发（用户点击退出按钮或课堂结束），SDK 会自动处理 RTC 引擎销毁并重载页面。

> **注意：** XDYClassroomAbility 由 HAR 模块提供，客户无需也无法直接修改其生命周期。如需在课堂退出时执行自定义逻辑，请联系学点云技术支持。

---

