# 学点云课堂 HarmonyOS SDK 集成文档

## 一、概述

学点云课堂 SDK（`@xdy/classroom`）是一个 HarmonyOS HAR 库，集成了实时音视频通讯、WebView 课堂展示和 Native RTC 引擎控制能力。客户只需在自己的 App 中引入组件即可启动课堂。

**核心特性：**
- 一行代码启动课堂，免登录进入
- 独立 UIAbility 运行，不影响客户 App 主流程
- 横屏全屏课堂体验
- 集成实时音视频（基于声网 Agora RTC）
- 支持屏幕共享、多人互动等教学场景

**技术要求：**
| 项目 | 要求 |
|------|------|
| 操作系统 | HarmonyOS 6.0.1+ |
| API Level | 21+ |
| 开发工具 | DevEco Studio 5.0+ |
| 设备类型 | 手机、平板 |

---

## 二、SDK 包内容

SDK 发布为一个自包含的 HAR 模块（`classroom.har`），内部已嵌入 xrtc 音视频引擎，客户只需引入一个包：

```
classroom.har (包含)
├── ClassroomView 组件（课堂核心 UI + 逻辑）
├── XDYClassroom 启动类
├── XDYClassroomAbility 模板
└── @av/xrtc 音视频引擎（内嵌依赖）
```

---

## 三、快速集成（5 步）

### 步骤 1：添加依赖

将 `classroom.har` 文件放入项目的 `libs/` 目录，然后在 entry 模块的 `oh-package.json5` 中添加依赖：

```json5
{
  "dependencies": {
    "@xdy/classroom": "file:./libs/classroom.har"
  }
}
```

然后执行 `ohpm install` 安装依赖。

### 步骤 2：创建课堂页面

在 `entry/src/main/ets/pages/` 目录下创建 `ClassroomPage.ets`：

```typescript
import { ClassroomView } from '@xdy/classroom';

const classroomStorage: LocalStorage = new LocalStorage();

@Entry(classroomStorage)
@Component
struct ClassroomPage {
  build() {
    ClassroomView()
  }
}
```

在 `entry/src/main/resources/base/profile/main_pages.json` 中注册该页面：

```json
{
  "src": [
    "pages/Index",
    "pages/ClassroomPage"
  ]
}
```

### 步骤 3：创建课堂 Ability

在 `entry/src/main/ets/entryability/` 目录下创建 `XDYClassroomAbility.ets`：

```typescript
import { AbilityConstant, UIAbility, Want } from '@kit.AbilityKit';
import { window } from '@kit.ArkUI';
import { hilog } from '@kit.PerformanceAnalysisKit';

const DOMAIN = 0x0000;

export default class XDYClassroomAbility extends UIAbility {
  private storage = new LocalStorage();

  onCreate(want: Want, launchParam: AbilityConstant.LaunchParam): void {
    hilog.info(DOMAIN, 'XDYClassroom', 'onCreate');
    const url = want.parameters?.classroomUrl as string ?? '';
    this.storage.setOrCreate('classroomUrl', url);
  }

  onDestroy(): void {}

  onWindowStageCreate(windowStage: window.WindowStage): void {
    // 横屏全屏
    windowStage.getMainWindow((err, win) => {
      if (!err.code) {
        win.setPreferredOrientation(window.Orientation.LANDSCAPE);
        win.setWindowLayoutFullScreen(true);
        win.setWindowSystemBarEnable([]);
      }
    });
    // 加载课堂页面，传入 LocalStorage（含课堂 URL）
    windowStage.loadContent('pages/ClassroomPage', this.storage, (err) => {
      if (err.code) {
        hilog.error(DOMAIN, 'XDYClassroom', 'Load failed: %{public}s', JSON.stringify(err));
      }
    });
  }

  onForeground(): void {}
  onBackground(): void {}
}
```

### 步骤 4：注册 Ability 并声明权限

在 `entry/src/main/module.json5` 中：

**注册 Ability：**
```json5
{
  "abilities": [
    // ... 您现有的 Ability
    {
      "name": "XDYClassroomAbility",
      "srcEntry": "./ets/entryability/XDYClassroomAbility.ets",
      "orientation": "landscape",
      "launchType": "standard"
    }
  ]
}
```

**声明权限：**
```json5
"requestPermissions": [
  { "name": "ohos.permission.INTERNET" },
  {
    "name": "ohos.permission.CAMERA",
    "reason": "$string:camera_reason",
    "usedScene": { "abilities": ["XDYClassroomAbility"], "when": "inuse" }
  },
  {
    "name": "ohos.permission.MICROPHONE",
    "reason": "$string:microphone_reason",
    "usedScene": { "abilities": ["XDYClassroomAbility"], "when": "inuse" }
  }
]
```

在 `resources/base/element/string.json` 中添加权限说明：
```json
{
  "string": [
    { "name": "camera_reason", "value": "用于课堂视频通话" },
    { "name": "microphone_reason", "value": "用于课堂语音通话" }
  ]
}
```

### 步骤 5：启动课堂

在您的业务页面中，调用 SDK 启动课堂：

```typescript
import { common } from '@kit.AbilityKit';
import { XDYClassroom } from '@xdy/classroom';

@Entry
@Component
struct YourPage {
  private context = getContext(this) as common.UIAbilityContext;

  build() {
    Column() {
      Button('进入课堂')
        .onClick(() => {
          // 传入完整的课堂 URL（含鉴权信息）
          const url = 'https://pclive.xuedianyun.com/xdyclass/?mobileWeb=true&token=xxx&courseId=xxx';
          XDYClassroom.launch(this.context, url);
        })
    }
  }
}
```

---

## 四、API 参考

### XDYClassroom

| 方法 | 说明 |
|------|------|
| `static launch(context, url, abilityName?)` | 启动课堂 |

**参数说明：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| context | UIAbilityContext | 是 | 调用方的 UIAbilityContext |
| url | string | 是 | 完整的课堂 H5 URL（含鉴权 token） |
| abilityName | string | 否 | 课堂 Ability 名称，默认 `'XDYClassroomAbility'` |

### ClassroomView

| 组件 | 说明 |
|------|------|
| `ClassroomView` | 课堂核心 UI 组件，包含 WebView、音视频渲染、JS Bridge 等全部功能 |

**使用方式：**
```typescript
import { ClassroomView } from '@xdy/classroom';

// 在 @Entry 页面中使用（需提供 LocalStorage）
@Entry(localStorage)
@Component
struct MyPage {
  build() {
    ClassroomView()
  }
}
```

**LocalStorage 参数：**

| Key | 类型 | 说明 |
|-----|------|------|
| classroomUrl | string | 课堂 H5 URL（可选，为空时使用默认登录页） |

---

## 五、课堂 URL 说明

课堂 URL 由您的后端服务生成，包含用户鉴权信息，用户无需在课堂内再次登录。

**URL 格式示例：**
```
https://pclive.xuedianyun.com/xdyclass/?mobileWeb=true&token=YOUR_AUTH_TOKEN&courseId=COURSE_ID&appVersion=v1.1.0
```

**常见参数：**

| 参数 | 说明 |
|------|------|
| mobileWeb | 固定为 `true`（移动端标识） |
| token | 用户鉴权 token（从后端获取） |
| courseId | 课程/课堂 ID |
| appVersion | 应用版本号（可选） |

> 具体 URL 参数请联系学点云技术支持获取对接文档。

---

## 六、架构说明

```
┌──────────────────────────────────────────────┐
│  客户 App                                     │
│  - 业务页面                                   │
│  - 点击按钮 → XDYClassroom.launch(url)       │
└─────────────────┬────────────────────────────┘
                  │ startAbility (Want)
                  ▼
┌──────────────────────────────────────────────┐
│  XDYClassroomAbility（独立窗口，横屏全屏）     │
│  ┌────────────────────────────────────────┐  │
│  │  ClassroomPage（薄包装 @Entry）         │  │
│  │  └─ ClassroomView (from HAR)           │  │
│  │     ┌────────────────────────────┐     │  │
│  │     │ WebView (课堂 H5 页面)      │     │  │
│  │     └──────────────┬─────────────┘     │  │
│  │                    │ JS Bridge          │  │
│  │     ┌──────────────▼─────────────┐     │  │
│  │     │ Native 控制层               │     │  │
│  │     │ - 音视频流管理              │     │  │
│  │     │ - 视图渲染 (XComponent)    │     │  │
│  │     │ - 屏幕共享                  │     │  │
│  │     └──────────────┬─────────────┘     │  │
│  │                    │                    │  │
│  │     ┌──────────────▼─────────────┐     │  │
│  │     │ xrtc SDK (内嵌于 HAR)       │     │  │
│  │     │ - RTC 引擎 (声网 Agora)    │     │  │
│  │     └────────────────────────────┘     │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

**关键特点：**
- 课堂运行在独立的 UIAbility 中，拥有自己的窗口生命周期
- ClassroomView 是 HAR 导出的 @Component，所有逻辑自包含
- 客户只需创建一个薄包装页面 + Ability 即可
- 退出课堂自动关闭 Ability，返回客户 App

---

## 七、退出课堂

课堂退出由 H5 页面内部触发（用户点击退出按钮或课堂结束），SDK 会自动处理 RTC 引擎销毁并重载页面。

如需监听课堂退出事件，可在 `XDYClassroomAbility` 的 `onDestroy()` 中添加自定义逻辑：

```typescript
onDestroy(): void {
  // 课堂已退出，可在此处执行清理或通知主 App
  hilog.info(DOMAIN, 'XDYClassroom', '课堂已退出');
}
```

---

## 八、常见问题

### Q1：课堂页面白屏/加载失败？
- 确认课堂 URL 可访问（浏览器中直接打开测试）
- 确认已声明 `ohos.permission.INTERNET` 权限
- 检查网络连接

### Q2：没有视频/音频？
- 确认已声明 `CAMERA` 和 `MICROPHONE` 权限
- 确认 `usedScene` 中包含了 `XDYClassroomAbility`
- 首次使用时系统会弹出权限申请弹窗，用户需同意

### Q3：ohpm install 后找不到模块？
- 确保在项目**根目录**执行 `ohpm install`
- 检查 oh-package.json5 中路径是否正确
- 执行 DevEco Studio 的 File → Sync and Refresh Project

### Q4：如何自定义课堂 Ability 名称？
- 修改 `module.json5` 中注册的 Ability `name` 字段
- 调用时传入第三个参数：`XDYClassroom.launch(ctx, url, 'MyAbilityName')`

### Q5：如何获取课堂 URL？
- 课堂 URL 由学点云后端服务生成
- 通常需要调用您后端的"获取课堂链接"接口
- 该接口返回带有鉴权 token 的完整 URL
- 具体接口文档请联系学点云技术支持

---

## 九、版本信息

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.1.0 | 2026-05-29 | SDK 化改造，ClassroomView 组件化导出，自包含 xrtc 引擎 |

---

**技术支持**: 如有问题，请联系学点云技术团队
